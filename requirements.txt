# Pinned by construction: versions frozen from the environment the tests run in
# (Python 3.11, Linux, CPU). torch is the CPU wheel; swap the index URL for CUDA.
exchange_calendars==4.13.2
matplotlib==3.11.0
numpy==2.4.6
pandas==2.3.3
pyarrow==25.0.0
pytest==9.1.1
pyts==0.13.0
PyWavelets==1.9.0
PyYAML==6.0.1
scikit-learn==1.7.2
scipy==1.17.1
sktime==1.0.1
statsmodels==0.14.6
tabulate==0.10.0
torch==2.13.0+cpu
xgboost==3.2.0
# Phase 3+ (installed when reached): torchvision, timm
